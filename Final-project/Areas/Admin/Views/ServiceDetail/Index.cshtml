@namespace Final_project.ViewModels.ServiceDetails

@model List<ServiceDetailVM>

<div class="container mt-5">
    @if (User.IsInRole("SuperAdmin"))
    {
        <div class="create">
            <a asp-area="Admin" asp-controller="ServiceDetail" asp-action="Create" class="btn btn-success text-white" style="font-size:30px;width:100px;">+</a>
        </div>
    }
    <table class="table">
        <thead>
            <tr>
                <th scope="col">Options</th>
                <th scope="col">Booking</th>
                <th scope="col">Luggage</th>
                <th scope="col">Service</th>
                <th scope="col">Operations</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Options</td>
                    <td>@item.Booking</td>
                    <td>@item.Luggage</td>
                    <td>@item.ServiceId</td>


                    <td>
                        <a class="btn btn-info text-white" asp-action="Detail" asp-route-id="@item.Id">Detail</a>
                        <a class="btn btn-warning text-white" asp-action="Edit" asp-route-id="@item.Id">Edit</a>
                        @if (User.IsInRole("SuperAdmin"))
                        {
                            <form class="form-sample d-inline" method="post" asp-action="Delete" asp-route-id="@item.Id">
                                <button type="submit" class="btn btn-danger text-white">Delete</button>
                            </form>
                        }
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>